curiobase_id:
  type: integer
  default: 106
nominations_id:
  type: integer
  default: 111
vault_id:
  type: integer
  default: 112
chronovisor_id:
  type: integer
  default: 85
min_voters:
  type: integer
  default: 1
scale_max:
  type: integer
  default: 5
label_x_left:
  type: string
  default: "SET DRESSING"
label_x_right:
  type: string
  default: "LOAD-BEARING"
label_y_bottom:
  type: string
  default: "TIDIED"
label_y_top:
  type: string
  default: "UNFLINCHING"
label_grid_hint:
  type: string
  default: "Each work sits where the community has scored it — centrality across, nerve up. Click a poster to open the work."
label_awaiting:
  type: string
  default: "not yet scored — be first"
label_community:
  type: string
  default: "Community"
label_chronostrip:
  type: string
  default: "From the Chronovisor"
label_challenge:
  type: string
  default: "CHALLENGE FILED"
label_response:
  type: string
  default: "EDITORIAL RESPONSE"
label_gravity:
  type: string
  default: "GRAVITY INDEX"
label_scorepanel_work:
  type: string
  default: "SCORE THIS PAIRING"
label_scorepanel_vault:
  type: string
  default: "RATE THIS WORK"
label_noworks:
  type: string
  default: "No works referenced yet — tag a Vault entry with this concept and give it a gravity block."
poll_labels:
  type: string
  default: "axis_x::DECORATION → OBSESSION::1 = the concept is set dressing · 5 = the work cannot exist without it|axis_y::CONTAINED → UNRESOLVED::1 = tidied into a neat ending · 5 = pursued past the point of comfort|accessibility::ACCESSIBILITY::1 = anyone can start here · 5 = demands homework|register::EMOTIONAL REGISTER::1 = cerebral · 5 = visceral|strangeness::STRANGENESS::1 = grounded · 5 = fully outside consensus reality|footprint::CULTURAL FOOTPRINT::1 = obscure · 5 = household name"
tier_colors:
  type: string
  default: "Essential:#2aa79b|Significant:#7fb8b0|Notable:#5a7a76"
screening_id:
  type: integer
  default: 12
label_nominate_cta:
  type: string
  default: "Nominate a work or concept →"
label_vote_cta:
  type: string
  default: "open the work & score it →"
label_vote_hint:
  type: string
  default: "tap to score"
label_discussion:
  type: string
  default: "DISCUSSION"
label_theconcept:
  type: string
  default: "THE CONCEPT"
label_positions:
  type: string
  default: "POSITIONS"
label_axis_x_short:
  type: string
  default: "CENTRALITY"
label_axis_y_short:
  type: string
  default: "NERVE"
label_axis_x_hint:
  type: string
  default: "is the idea load-bearing? 1 = set dressing · 5 = the work cannot exist without it"
label_axis_y_hint:
  type: string
  default: "how far does it follow the idea? 1 = tidied into a neat ending · 5 = pursued past the point of comfort"
label_challenge_btn:
  type: string
  default: "challenge this score"
label_permalink:
  type: string
  default: "permalink"
label_voter:
  type: string
  default: "voter"
label_voters:
  type: string
  default: "voters"
label_yourscore:
  type: string
  default: "your score"
label_scorebtn:
  type: string
  default: "score this pairing ↓"
label_fiction:
  type: string
  default: "FICTION"
label_nonfiction:
  type: string
  default: "NONFICTION · documentary, explainer, investigation"
label_score_prefix:
  type: string
  default: "SCORE THIS WORK AGAINST"
label_counting:
  type: string
  default: "…"
label_work:
  type: string
  default: "work"
label_works:
  type: string
  default: "works"
label_noworks_short:
  type: string
  default: "no works yet — add one"

# ── entry system ────────────────────────────────────────────────────────────
# Format: slug:DISPLAY LABEL:schema.org type, separated by |
entry_types:
  type: string
  default: "event:EVENT:Event|person:PERSON:Person|place:PLACE:Place|object:OBJECT:CreativeWork|org:ORGANISATION:Organization|claim:CLAIM:Claim|concept:CONCEPT:DefinedTerm|work:WORK:CreativeWork"
label_entry_needs:
  type: string
  default: "THIS ENTRY NEEDS"
label_entry_needs_hint:
  type: string
  default: "Know one of these? Reply below — or tick it once it's been added."
label_entry_complete:
  type: string
  default: "NOTHING OUTSTANDING"
label_entry_connected:
  type: string
  default: "Connected"
label_entry_reviewed:
  type: string
  default: "Last checked"
# Which row label in the fact table carries the review date (pulled out of the strip)
label_entry_reviewed_key:
  type: string
  default: "Reviewed"
label_entry_needs_cta:
  type: string
  default: "Reply with what you know"
entries_id:
  type: integer
  default: 0
label_nav_entries:
  type: string
  default: "Entries"
label_entry_index_title:
  type: string
  default: "THE ENTRIES"
label_entry_index_hint:
  type: string
  default: "One page per subject. Anything with an open question is asking for help."
label_entry_index_empty:
  type: string
  default: "No entries yet."
label_entry_contributions:
  type: string
  default: "contributions"
entry_gap_reaction:
  type: string
  default: "mag"
label_entry_gap_one:
  type: string
  default: "1 member has flagged this entry as needing a source."
label_entry_gap_many:
  type: string
  default: "%{count} members have flagged this entry as needing a source."
